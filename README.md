# 📂 Automated File Organizer

A Python-based utility that monitors a directory and automatically categorizes files into organized subfolders based on their file extensions.

## ✨ Features
- **Instant Organization**: Moves files into folders like `Images`, `Documents`, and `Scripts`.
- **Clean Code**: Uses Python's built-in `os` and `shutil` libraries.
- **Easy Setup**: No external dependencies required.

## 🚀 How to Run
1. Clone this repo: `git clone https://github.com/jubairae10/automated-file-organizer.git`
2. Place `organizer.py` in the folder you want to clean.
3. Run the script:
   ```bash
   python organizer.py
